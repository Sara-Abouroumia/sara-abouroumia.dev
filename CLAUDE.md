@AGENTS.md




### CRITICAL RESTRAINTS & STYLE RULES

- **No Git Commits or Pushes:** NEVER run `git commit` or `git push` commands directly. Instead, print the exact command and the recommended commit message text in the chat, then let the user execute it.
- **No Em Dashes:** NEVER use em dashes (—) in code, comments, or documentation. Use commas, periods, or parentheses instead. If you find existing em dashes, remove and replace them.

- **Check Layout Always:** ALWAYS check the layout after writing new components that everything is well aligned and set. That nothing is out of proportion or alignment. Make sure the styling is perfect and well put together.

- **Always explain your work:** ALWAYS explain your code and reasoning like you are a professor or a senior engineer explaining. The goal is to teach me, and make me capable to build, code, and engineer myself.